# iot-bridger

IoT protocol bridge service that translates between device protocols (MQTT, CoAP, HTTP, WebSocket) and normalizes messages into a unified data model for downstream consumers.

## Project structure

```
.agents/          # Agent role definitions, shared context, and workflow guides
docs/
  adr/            # Architecture Decision Records
  architecture/   # System design, data flow, component diagrams
  design/         # Technical design specs per feature/module
  integrations/   # Per-integration setup and behavior docs
  protocols/      # IoT protocol references and bridging rules
  user-experience/ # UX flows, personas, API consumer guides
```

## Key conventions

<!-- Add: language/runtime, test commands, lint commands, build commands -->

## Domain glossary

<!-- Add: project-specific terms agents should understand consistently -->
<!-- Example: "bridge" = a configured translation pathway between two protocol endpoints -->
