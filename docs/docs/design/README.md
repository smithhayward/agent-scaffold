# Design Specs

Per-feature technical design documents. Each file is produced by the Architect agent and approved before implementation begins.

## Naming convention

`<feature-or-module-name>.md` — e.g., `mqtt-adapter.md`, `message-router.md`

## Status field

Each doc should open with a status line:

```
Status: Draft | Approved | Implemented | Superseded
```
