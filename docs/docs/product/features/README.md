# Features

One file per epic, breaking it into discrete shippable features. File naming: `EP-NN-short-title.md`.

Features should be written at the level of: "a user can do X, which produces Y." Implementation detail belongs in `docs/design/`.

## Index


